/// Tap-routing payload for a salat-time notification: opens the Prayer Times
/// sheet (today's five times, the one that just fired highlighted) rather
/// than leaving the tap as a no-op.
const String openPrayerTimesPayload = 'open:prayer_times';
