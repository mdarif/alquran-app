/// Notification tap-routing payloads. v1: only the Al-Kahf reminder routes into
/// the app (opens Surah 18); all other reminders are informational (no payload).
const String openAlKahfPayload = 'open:alkahf';
