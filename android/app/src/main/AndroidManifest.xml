<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Prayer times: device location to compute the daily schedule on-device.
         Coarse is enough (city-level); fine sharpens accuracy near borders.
         Re-apply after a `flutter create` regen — see `make location-perms`. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Sunnah reminders (local notifications). No SCHEDULE_EXACT_ALARM — we
         use inexact scheduling. Re-apply after a `flutter create` regen — see
         `make notif-perms`. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <application
        android:label="Al Quran"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Disable Impeller on Android. Impeller (default for Vulkan devices since
             Flutter 3.27) does not apply the KFGQPC font's Arabic GPOS mark/mkmk
             anchors, so the elongated madd (يَـٰٓأَيُّهَا) collapses. Skia renders it
             correctly. Mirrors FLTEnableImpeller=false in ios/Runner/Info.plist.
             See LEARNINGS.md §1. Re-apply after any `flutter create` regen (run
             `make disable-impeller`). -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
        <!-- "Next prayer" home-screen widget. Data is pushed from Dart via the
             home_widget plugin (WidgetPublisher); this receiver only renders.
             Re-apply after any `flutter create` regen. -->
        <receiver
            android:name=".PrayerWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/prayer_widget_info" />
        </receiver>
        <!-- Wide "today's prayers" widget (all five salah). Same payload, second
             renderer. Re-apply after any `flutter create` regen. -->
        <receiver
            android:name=".PrayerScheduleWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/prayer_schedule_widget_info" />
        </receiver>
        <!-- Sunnah reminders (flutter_local_notifications): re-arm pending
             notifications after a reboot / app update. Re-apply after a
             `flutter create` regen — see `make notif-perms`. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
