<?xml version="1.0" encoding="utf-8"?>
<!-- "Next prayer" home-screen widget (compact, 2 lines). The prayer name is a
     tight caps label; the TIME is the hero. Both texts are set at runtime by
     PrayerWidgetProvider — the android:text values are picker-preview defaults. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/prayer_widget_bg"
    android:padding="12dp">

    <TextView
        android:id="@+id/widget_prayer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:textAllCaps="true"
        android:letterSpacing="0.14"
        android:textColor="#8FB3AA"
        android:textSize="12sp"
        android:text="Asr" />

    <TextView
        android:id="@+id/widget_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:maxLines="1"
        android:textColor="#F4F1E9"
        android:textSize="28sp"
        android:textStyle="bold"
        android:text="3:30 PM" />
</LinearLayout>
